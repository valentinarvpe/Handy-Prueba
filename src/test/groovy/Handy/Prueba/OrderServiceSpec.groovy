package Handy.Prueba

import grails.testing.services.ServiceUnitTest
import spock.lang.Specification

class OrderServiceSpec extends Specification implements ServiceUnitTest<OrderService> {

    /*
    void "test saveOrder method"() {
        List orders = []
        Map obj = new HashMap()
        obj.put("id", 1)
        obj.put("sellerComment", "TESTING")
        obj.put("totalSales", "1200001")
        orders.add(obj)
        expect:
        service.saveOrders(orders)
    }
    */
}
